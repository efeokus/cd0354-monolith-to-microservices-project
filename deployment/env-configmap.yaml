apiVersion: v1
kind: ConfigMap
metadata:
  name: env-config
  
data:
  POSTGRES_USERNAME: postgres
  POSTGRES_PASSWORD: mypassword
  POSTGRES_HOST: mydatabase.caqepxfks0wt.us-east-2.rds.amazonaws.com
  POSTGRES_DB: postgres
  AWS_BUCKET: efeoawsbucket
  AWS_REGION: us-east-2
  AWS_PROFILE: default
  JWT_SECRET: testing
  URL: http://localhost:8100
